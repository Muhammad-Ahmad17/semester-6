transport layer
how transport layer detect error 
    reciver side transport layer check if the data isnt desired it send signal to transmiter to resend the date
appplication layer give data to transport layer
osi model / tcp ip , explode at every layer 
twisted pair cable use case
    crossover 
    straight 
switch 
    cutoff
    straight
encaps , decaps
port no 
    smtp  25
    ftp
    http  80
    dns   53 
    pop3  110
dns is udp 
push and pull type , smtp and pop